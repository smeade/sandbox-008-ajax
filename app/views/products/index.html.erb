<div class="page-header">
  <%= link_to new_product_path, :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    New Product
  <% end %>
  <h1>Listing products</h1>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th>Sku</th>
        <th>Name</th>
        <th>Description</th>
        <th>Price cents</th>
        <th>Active</th>
        <th>Available on</th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, @products) do |product| %>
        <td><%= link_to product.sku, product %></td>
        <td><%= link_to product.name, product %></td>
        <td><%= product.description %></td>
        <td><%= product.price_cents %></td>
        <td><%= product.active %></td>
        <td><%= product.available_on %></td>
        <td>
          <%= render partial: 'reviews/reviews', locals: {reviews: product.reviews, product_id: product.id} %>
          <b><%= link_to 'Reviews', '#', data: {toggle: "modal", target:"#product-#{product.id}-reviews" } %></b>
        </td>
        <td><%= link_to 'Add Review', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      <% end %>
    </tbody>
  </table>
</div>
